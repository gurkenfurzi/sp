/* Studia V274 Google device sync configuration.
   Optional: paste your deployed Apps Script /exec URL below.
   If left blank, Studia uses the URL saved in Settings on this device. */
window.STUDIA_SYNC_CONFIG = window.STUDIA_SYNC_CONFIG || {
  scriptUrl: "",
  googleClientId: ""
};
